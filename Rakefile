# -*- encoding: utf-8 -*-

require 'jeweler'
Jeweler::Tasks.new do |s|
  s.name = "rfc822"
  s.version = '0.1.0'
  s.authors = ["Marcin Lewandowski"]
  s.email = ["edek123456@gmail.com"]
  s.summary = %q{Gem containing just e-mail validation regexp based on RFC822, please use RFC822::EMAIL_REGEXP_WHOLE for checking if whole string is an valid email, and RFC822::EMAIL_REGEXP_PART for e.g. scanning a string for e-mail address extraction.}
  s.description = %q{Gem containing just e-mail validation regexp based on RFC822, please use RFC822::EMAIL_REGEXP_WHOLE for checking if whole string is an valid email, and RFC822::EMAIL_REGEXP_PART for e.g. scanning a string for e-mail address extraction.}
  s.homepage = "http://github.com/saepia/rfc822"
  s.files = ["Gemfile", "rfc822.gemspec", "lib/", "lib/rfc822.rb"]
  s.require_paths = ["lib"]

end
Jeweler::RubygemsDotOrgTasks.new

